use chora::{Tensor, TensorShape};

#[test]
fn test_name_here() {
    // 1. build a TensorShape, e.g. shape [2, 3]
    // 2. build a Tensor with mut binding and some known data, e.g. 0..6
    // 3. mutate one element via index_mut: tensor[&[r, c]] = some_value;
    // 4. read it back: assert_eq!(tensor[&[r, c]], some_value);
}
